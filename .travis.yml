language: android
android:
  components:
    - build-tools-26.0.2
  licenses:
    - 'android-sdk-preview-license-52d11cd2'
    - 'android-sdk-license-.+'
    - 'google-gdk-license-.+'

before_install:
  - cd Android
  - chmod +x gradlew

script:
  - ./gradlew assemble
